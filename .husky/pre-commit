echo "Running lint-staged (lint + format on staged files) before commit..."

if ! pnpm exec lint-staged; then
  echo ""
  echo "✖ lint-staged failed. Commit aborted."
  echo "  Fix the issues above, or run 'pnpm run format' to auto-fix formatting."
  exit 1
fi
