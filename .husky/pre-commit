echo "Running pnpm run check (lint + typecheck + format) before commit..."

if ! pnpm run check; then
  echo ""
  echo "✖ pnpm run check failed. Commit aborted."
  echo "  Fix the issues above, or run 'pnpm run format' to auto-fix formatting."
  exit 1
fi
